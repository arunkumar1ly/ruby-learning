# # ruby 2.3.0
# # About Class nested inside another Class

# classes nested inside of modules are generally used to namespace the classes. For instance, Although this is not the only use of classes nested inside of modules, it is generally the most common.
module ActiveRecord
  class Base

  end
end